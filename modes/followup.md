# followup — SCAFFOLD (not built yet)

Scaffolded to mirror career-ops's `modes/followup.md` (+ `followup-cadence.mjs`) so
it's drop-in later. **Not implemented in this MVP.** If asked: say it's planned,
don't invent a schedule, and offer the core flow instead.

Intent when built: compute follow-up cadence for jobs the user actually applied to
(from `data/feedback.md`, action `applied`) and flag overdue nudges.
Inputs already present: `data/feedback.md` (applied entries + dates). To add: a cadence tool.
