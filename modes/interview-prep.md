# interview-prep — SCAFFOLD (not built yet)

Scaffolded to mirror career-ops's `modes/interview-prep.md` so it's drop-in later.
**Not implemented in this MVP.** If asked: say it's planned, do NOT fabricate
intel, and offer the core flow instead.

Intent when built: a company-specific prep guide for a scored job — likely questions,
STAR+R stories drawn from `resume.md`, and gaps to address (the rubric's `missing`/
`partial` qualifications are the honest starting point).
Inputs already present: the job + verdict in `data/results/scored.jsonl`, `resume.md`, `config/profile.yml`.
