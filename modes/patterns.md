# patterns — SCAFFOLD (not built yet)

Scaffolded to mirror career-ops's `modes/patterns.md` (+ `analyze-patterns.mjs`) so
it's drop-in later. **Not implemented in this MVP.** If asked: say it's planned,
don't fabricate analysis, and offer the core flow instead.

Intent when built: analyze the user's corrections + scored history to improve
targeting — e.g. which functions/locations/comp bands they keep rejecting — and
suggest profile tweaks. Honest signal only; never overfit a handful of points.
Inputs already present: `data/feedback.md`, `data/results/scored.jsonl`, `data/tracker.md`.
