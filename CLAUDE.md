@AGENTS.md

<!--
Claude Code reads AGENTS.md (canonical) via the import above. Nothing Claude-specific
is needed here. Status: multi-LLM (non-Claude) is UNVERIFIED until run on a non-Claude
CLI — see README "Status". The headless `python -m jobfinder --resume … --cli …` path
is CI/batch-only, not for interactive use.
-->
