@AGENTS.md

<!--
Claude Code reads AGENTS.md (canonical) via the import above. Nothing Claude-specific
is needed here. Status: multi-LLM VERIFIED in-session on Claude + GitHub Copilot — see
README "Status". The headless `python -m jobfinder --resume … --cli …` path is
CI/batch-only, not for interactive use.
-->
